{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/FrontendApp/browser",
  "installCommand": "npm install && npm run setup-bootstrap",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
